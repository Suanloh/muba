{
  "name": "@mova/wallet",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "MOVA Sui wallet layer — ownership model, execution gate (safety boundary), network handling, provider abstraction. Framework-agnostic; the web app adapts it to @mysten/dapp-kit.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "tsx --test src/*.test.ts"
  },
  "dependencies": {
    "@mova/config": "*",
    "@mova/logger": "*",
    "@mova/types": "*",
    "@mysten/sui": "^2.27.0"
  },
  "devDependencies": {
    "tsx": "^4.23.12"
  }
}
