{
  "name": "@mova/ai",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "MOVA AI layer — natural-language payment parsing. Proposal-only: converts NL into structured, schema-validated intents. Never produces executable transaction instructions, never executes, never approves.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "tsx --test src/*.test.ts"
  },
  "dependencies": {
    "@mova/core": "*",
    "@mova/logger": "*",
    "@mova/types": "*"
  },
  "devDependencies": {
    "tsx": "^4.23.12"
  }
}
